---------------- CHECKBOX ----------------------------------------------

in checkbox there is no text so there cant be 
e.target.value instead a checkbox be be checked or 
unchecked so there is [ e.target.checked ] in case of checkbox
yaha e.target.checked hota hai 

two way binding in check box 

const [completed, setCompleted] = useState(false)

<input
    checked={completed}
    onChange={(e)=>setCompleted(e.target.checked)}
    type="checkbox" 
/>Completed



// REACT TOSTIFY AND REACT HOOK FORMS
React   Hook Form : for validation and two way binding 



include react toast container in main.jsx , wrap it along with app.jsx <toastContainer/>
and then use it where its needed toast.sucess("this is my message")   toast.error("This is my error")